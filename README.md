# Trip Hub

Shared trip planning, itinerary, food, places, and travel-assistant app.

## Local checks

- `npm run check` runs lint, unit tests, and a production build.
- `npm run test:e2e` runs the browser smoke tests.
