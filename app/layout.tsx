import './globals.css';
export const metadata={title:'Toronto Trip Hub',description:'Shared Toronto, Niagara Falls and Buffalo trip planner'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
