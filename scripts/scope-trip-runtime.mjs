import fs from 'node:fs';
const path='components/TripApp.tsx';
let text=fs.readFileSync(path,'utf8');
if(!text.includes("from '@/lib/active-trip'"))text=text.replace("import {formatPrepDueDate,nextPrepTask,prepDueStatus,prepTasks} from '@/lib/trip-prep';", "import {formatPrepDueDate,nextPrepTask,prepDueStatus,prepTasks} from '@/lib/trip-prep';\nimport {activeTripId,stateApiUrl} from '@/lib/active-trip';\nimport {scopedStorageKey} from '@/lib/client-state';");
if(!text.includes("from '@/components/TripSwitcher'"))text=text.replace("import AddToDayPanel from '@/components/AddToDayPanel';", "import AddToDayPanel from '@/components/AddToDayPanel';\nimport TripSwitcher from '@/components/TripSwitcher';");
if(!text.includes("from '@/components/TripOnboarding'"))text=text.replace("import TripSwitcher from '@/components/TripSwitcher';", "import TripSwitcher from '@/components/TripSwitcher';\nimport TripOnboarding from '@/components/TripOnboarding';");
if(!text.includes("from '@/components/ItineraryImport'"))text=text.replace("import TripOnboarding from '@/components/TripOnboarding';", "import TripOnboarding from '@/components/TripOnboarding';\nimport ItineraryImport from '@/components/ItineraryImport';");
if(!text.includes("from '@/components/BulkLocationMatcher'"))text=text.replace("import ItineraryImport from '@/components/ItineraryImport';", "import ItineraryImport from '@/components/ItineraryImport';\nimport BulkLocationMatcher from '@/components/BulkLocationMatcher';");
if(!text.includes("from '@/components/PlanningHealth'"))text=text.replace("import BulkLocationMatcher from '@/components/BulkLocationMatcher';", "import BulkLocationMatcher from '@/components/BulkLocationMatcher';\nimport PlanningHealth from '@/components/PlanningHealth';");
if(!text.includes("from '@/components/TripCollaboration'"))text=text.replace("import PlanningHealth from '@/components/PlanningHealth';", "import PlanningHealth from '@/components/PlanningHealth';\nimport TripCollaboration from '@/components/TripCollaboration';");
if(!text.includes("from '@/components/AccountAccess'"))text=text.replace("import TripCollaboration from '@/components/TripCollaboration';", "import TripCollaboration from '@/components/TripCollaboration';\nimport AccountAccess from '@/components/AccountAccess';");
text=text.replaceAll("fetch('/api/state'", "fetch(stateApiUrl()");
text=text.replaceAll('readLocalState(localStorage)', 'readLocalState(localStorage,activeTripId())');
text=text.replaceAll('pushCloudState(local)', 'pushCloudState(local,fetch,activeTripId())');
text=text.replaceAll('pushCloudState(next)', 'pushCloudState(next,fetch,activeTripId())');
text=text.replaceAll('stageDeviceState(localStorage,next)', 'stageDeviceState(localStorage,next,activeTripId())');
text=text.replaceAll('markCloudSynced(localStorage)', 'markCloudSynced(localStorage,new Date().toISOString(),activeTripId())');
text=text.replaceAll('localStorage.getItem(pendingSyncKey)', 'localStorage.getItem(scopedStorageKey(pendingSyncKey,activeTripId()))');
text=text.replaceAll('localStorage.removeItem(pendingSyncKey)', 'localStorage.removeItem(scopedStorageKey(pendingSyncKey,activeTripId()))');
text=text.replaceAll('localStorage.getItem(lastSyncKey)', 'localStorage.getItem(scopedStorageKey(lastSyncKey,activeTripId()))');
text=text.replaceAll('localStorage.setItem(localStateKey,JSON.stringify(selected))', 'localStorage.setItem(scopedStorageKey(localStateKey,activeTripId()),JSON.stringify(selected))');
text=text.replaceAll('localStorage.setItem(localStateKey,JSON.stringify(loaded.state))', 'localStorage.setItem(scopedStorageKey(localStateKey,activeTripId()),JSON.stringify(loaded.state))');
text=text.replaceAll("localStorage.setItem('trip-state',JSON.stringify(next))", 'localStorage.setItem(scopedStorageKey(localStateKey,activeTripId()),JSON.stringify(next))');
text=text.replace('<div className="headerActions"><span className={`sync syncStack', '<div className="headerActions"><TripSwitcher/><span className={`sync syncStack');
text=text.replace("{editorView?<button className=\"btn ghost\" onClick={lockEditor}>Lock editing</button>:!isEditor&&<button className=\"btn ghost\" onClick={()=>{setAuthError('');setShowEditorUnlock(true);}}>Editor access</button>}", "{!editorView&&!isEditor&&<button className=\"btn ghost\" onClick={()=>{setShareMessage('');setShowSharePanel(true);}}>Sign in to edit</button>}");
text=text.replace("<button className=\"textButton\" onClick={()=>setShowEditorUnlock(true)}>Unlock editing</button>", "<button className=\"textButton\" onClick={()=>{setShareMessage('');setShowSharePanel(true);}}>Sign in to edit</button>");
const mainMarker='<main className={`shell ${editorView?\'editorMode\':\'viewerMode\'}`}>\n';
if(!text.includes('<TripOnboarding state={state}'))text=text.replace(mainMarker,`${mainMarker}   {editorView&&!tripSettings.onboardingCompleted&&state.days.flatMap(day=>day.items).length===0&&state.places.length===0&&state.foods.length===0&&state.packing.length===0&&<TripOnboarding state={state} onComplete={next=>void persist(next)}/>}\n`);
if(!text.includes('<ItineraryImport state={state}'))text=text.replace('<div className="placeActions">{boardUndo&&<button className="btn" onClick={undoBoardChange}>↶ Undo planning change</button>}', '<div className="placeActions"><ItineraryImport state={state} onApply={next=>void persist(next)}/><BulkLocationMatcher state={state} onApply={next=>void persist(next)}/>{boardUndo&&<button className="btn" onClick={undoBoardChange}>↶ Undo planning change</button>}');
else if(!text.includes('<BulkLocationMatcher state={state}'))text=text.replace('<ItineraryImport state={state} onApply={next=>void persist(next)}/>', '<ItineraryImport state={state} onApply={next=>void persist(next)}/><BulkLocationMatcher state={state} onApply={next=>void persist(next)}/>');
text=text.replace("{tab==='Overview'&&<TripOverview state={state} settings={tripSettings} canEdit={editorView} onToday={()=>setTab('Today')} onBrief={index=>{setBriefDayIndex(index);setShowDailyBrief(true);}} onEditDay={()=>setTab('Itinerary')} onChecklist={()=>setTab('Checklist')}/>}" , "{tab==='Overview'&&<TripOverview state={state} settings={tripSettings} canEdit={editorView} onToday={()=>setTab('Today')} onBrief={index=>{setBriefDayIndex(index);setShowDailyBrief(true);}} onEditDay={()=>setTab('Itinerary')} onChecklist={()=>setTab('Checklist')} onPlanningNavigate={target=>setTab(target as Tab)}/>}");
text=text.replace("function TripOverview({state,settings,canEdit,onToday,onBrief,onEditDay,onChecklist}:{state:TripState;settings:TripSettings;canEdit:boolean;onToday:()=>void;onBrief:(dayIndex:number)=>void;onEditDay:(date:string)=>void;onChecklist:()=>void}){", "function TripOverview({state,settings,canEdit,onToday,onBrief,onEditDay,onChecklist,onPlanningNavigate}:{state:TripState;settings:TripSettings;canEdit:boolean;onToday:()=>void;onBrief:(dayIndex:number)=>void;onEditDay:(date:string)=>void;onChecklist:()=>void;onPlanningNavigate:(target:any)=>void}){");
if(!text.includes('<PlanningHealth state={state}'))text=text.replace('  <div className="overviewStats"><div><strong>{state.days.length}</strong><span>trip days</span></div><div><strong>{fixedPlans}</strong><span>timed anchors</span></div><div><strong>{state.places.length}</strong><span>saved places</span></div></div>', '  <div className="overviewStats"><div><strong>{state.days.length}</strong><span>trip days</span></div><div><strong>{fixedPlans}</strong><span>timed anchors</span></div><div><strong>{state.places.length}</strong><span>saved places</span></div></div>\n  {canEdit&&<PlanningHealth state={state} onNavigate={onPlanningNavigate}/> }');
if(!text.includes('<TripCollaboration/>'))text=text.replace('<div className="shareAccessGrid">','<TripCollaboration/><div className="shareAccessGrid">');
if(!text.includes('<AccountAccess tripId={activeTripId()}/>'))text=text.replace('<TripCollaboration/>','<TripCollaboration/><AccountAccess tripId={activeTripId()}/>');
fs.writeFileSync(path,text);
