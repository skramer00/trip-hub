'use client';import Link from 'next/link';import {useState} from 'react';
export default function Login(){const [password,setPassword]=useState('');const [error,setError]=useState('');async function login(e:React.FormEvent){e.preventDefault();setError('');const r=await fetch('/api/auth',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password})});if(r.ok)location.reload();else setError('That legacy owner PIN did not work.')}return <main className="login"><div className="card"><div className="eyebrow">LEGACY RECOVERY</div><h1>Trip Hub owner recovery</h1><p className="muted">Normal editing now uses your Trip Hub account. Use this only to recover an older trip that has not yet been attached to an account.</p><p><Link className="btn primary" href="/trips">Sign in with your account</Link></p><details><summary>Use legacy owner PIN</summary><form onSubmit={login}><label>Legacy owner PIN<input className="field" type="password" inputMode="numeric" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="error">{error}</div>}<button className="btn">Open temporary recovery session</button></form></details></div></main>}
