'use client';import {useState} from 'react';
export default function Login(){const [password,setPassword]=useState('');const [error,setError]=useState('');async function login(e:React.FormEvent){e.preventDefault();setError('');const r=await fetch('/api/auth',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password})});if(r.ok)location.reload();else setError('That password did not work.')}return <main className="login"><div className="card"><h1>Toronto Trip Hub</h1><p className="muted">Shared itinerary, food list, saved places, and packing checklist.</p><form onSubmit={login}><label>Shared password<input className="field" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoFocus/></label>{error&&<div className="error">{error}</div>}<button className="btn primary">Open trip</button></form></div></main>}
